# ----- Segredos / ambiente (NUNCA versionar) -----
.env
alertmanager/alertmanager.yml

# ----- Arquivos locais -----
*.local
.DS_Store
Thumbs.db

# ----- Canário de segurança (runtime, não versionar) -----
security-exporter/textfile/
security-exporter/coleta.log
security-exporter/grafana-pass.txt
security-exporter/whitelist.ps1
Documento-Grafana/README.md

# ----- Docker volumes / dados -----
# (nada aqui no momento, mas deixa preparado)